
/**
 * Get the application name and version number
 * May be better as application-specific as other info (eg. price key version) may be included
 * Overlaps with getAppSet
 * @returns { appname, appversion }
 */
getVersion 

/**
 * Returns application's config/settings
 * @returns settings object
 */
getAppSet


class VHPcontroller{
    constructor(){

    }

    addRoute(server,route){
        server.on(route)
    }
}